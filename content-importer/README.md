# Eno Workbench Content Importer

在 WordPress 后台上传并安装 `eno-workbench-content-importer.zip`，激活后会幂等创建或更新 16 篇正式文章、真实分类和对应标签，并按站点时区写入 2020—2026 的发布时间。再次激活只会按固定 slug 更新，不会删除其他文章或创建账号。激活成功后后台顶部会显示 16 篇文章链接；默认 Hello World 仅在标题和正文都严格匹配时改为草稿。
